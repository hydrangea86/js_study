

for (var level = 2; level <= 9; level++) {
    if (level % 2 === 0) {
        for (var line = 1; line <= 9; line++) {
            console.log(`${level} x ${line} = ${level * line}`);
        }
        console.log(`=====================================`);
    }

}


var num = 1;

for (var k = 0; k < 2; k++) {
    for (var j = 0; j < 3; j++) {
        for (var i = 0; i < 5; i++) {
            
        }
        console.log(`안녕` + num++);
    }

}
